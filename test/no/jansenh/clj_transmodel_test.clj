(ns no.jansenh.clj-transmodel-test
  (:require [clojure.test :refer [deftest is testing]]
            [no.jansenh.clj-transmodel :as sut])) ; system under test

(deftest a-test
  (testing "FIXME, I fail."
    (is (= 1 1))))
